machine:
  services:
    - docker

dependencies:
  pre:
    - pip install docker-compose
    - docker-compose build

test:
  pre:
    - docker-compose up -d
  override:
    - docker-compose run web npm test
