web:
  build: .
  volumes:
    - "./src:/server/src"
  ports:
    - "3000:3000"
  env_file:
    - "./scripts/environment.env"
  links:
    - db
db:
  image: postgres:9.4
  environment:
    POSTGRES_PASSWORD: passwd
    POSTGRES_USER: dbuser
