
/**
 * a function that should validate a facebook token
 */
export function validate() {
  // https://graph.facebook.com/me?access_token={accessToken}
  // https://www.googleapis.com/oauth2/v1/tokeninfo?access_token={accessToken}
  //
}
